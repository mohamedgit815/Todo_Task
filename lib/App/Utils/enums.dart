enum RequestsState {initial, loading, success, failure, local}
enum UserEnum {user, admin,driver}
enum DiscountTypeEnum {flat, percentage}

enum SecureStorageEnum {
  preferencesTheme ,
  setToken ,
  setUserId ,
  setUserName ,
  setUserEmail ,
  setIsAdmin ,
  setIsActive ,
  createdAt ,
  updatedAt ,
}

enum LangEnum {
  ar , en , es , lang ,
  /// Appbar
  addItem , update , home , dark , light , add , noAdd , noItem , wait ,
  mic , title , content , loading , create , permission , internet , success ,
  checkOut , totalCount, total, cart, bestProduct, search , price, reBack ,
  email , cartAdded, payAndLocation, addLocation, addToCart,failed ,
  removeCart, purchase, order, yourLocation, ok, yes, no, currentLocation,
  favorites, noData, sortByPrice, lowToHigh, highToLow, filterByBrand ,
  availableStock, availableColor, availableSize, about, size, outOfStock,
  off , addDiscount, searchItem, names, chooseCategory, logOut, areUSure,
  dashboard, delete, slideToSubmit, slideToPay, cancel, status, count,
  pending, shipped, delivered, cancelled ,processing, phone, payments
  //Available Stock
}